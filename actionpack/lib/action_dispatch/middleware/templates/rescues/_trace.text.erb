Quails.root: <%= defined?(Quails) && Quails.respond_to?(:root) ? Quails.root : "unset" %>

<% @traces.each do |name, trace| %>
<% if trace.any? %>
<%= name %>
<%= trace.map { |t| t[:trace] }.join("\n") %>

<% end %>
<% end %>
